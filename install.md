
Dependency library:
blas/lapack	A Linear Math Library for calculate matrix transform, need install with binary from yum or orthers
opencv245	A image process library, we use it for read image and write video, need to compile from source
ffmpeg		A collections of video codec library, opencv need it's video code to encode a video. need to compile from source and a static library not suitable for opencv
glog		A log engine, whole log file writen in "/tmp" directory, need to compile from source

